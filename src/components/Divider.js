import React from 'react';

const HR = () => {
    return (
        <hr style={{
            color: 'var(--accent)',
            backgroundColor: 'var(--accent)',
            height: 1,
            borderColor: 'var(--accent)'
        }} />
    )
}

export default HR;